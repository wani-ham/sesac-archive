<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <style>
        span {
          color: blue;
          font-weight: 700;
        }
      </style>
</head>
<body>
    <h1><%= title %></h1>
    <div>Welcome <span><%= userInfo.id %></span> !</div>
    <div>Your password is <span><%= userInfo.pw %></span> !!</div>
    <div>Your email is <span><%= userInfo.email %></span> !!</div>
    <% if (userInfo.grade != "") { %>
      <div>Your grade is <span><%= userInfo.grade %></span> !!</div>
    <% } %>
    <br><br>
    <a href="/">Back to Home</a>
</body>
</html>